# =============================================================================
# Amorce Orchestrator Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# MODE SELECTION
# -----------------------------------------------------------------------------
# Options: standalone | cloud
# - standalone: Uses local files, no cloud dependencies (default)
# - cloud: Connects to Amorce Trust Directory and cloud services
AMORCE_MODE=standalone

# -----------------------------------------------------------------------------
# STANDALONE MODE SETTINGS
# -----------------------------------------------------------------------------
# Used when AMORCE_MODE=standalone

# Path to local agent registry (default: ./config/agents.json)
# LOCAL_AGENTS_FILE=./config/agents.json

# Path to local services registry (default: ./config/services.json)
# LOCAL_SERVICES_FILE=./config/services.json

# Path to SQLite database for transaction logs (default: ./data/transactions.db)
# LOCAL_DB_PATH=./data/transactions.db

# Local agent identity (if running as an agent)
# LOCAL_AGENT_ID=agent-local-001
# LOCAL_PRIVATE_KEY_FILE=./agent_private_key.pem

# -----------------------------------------------------------------------------
# CLOUD MODE SETTINGS
# -----------------------------------------------------------------------------
# Only used when AMORCE_MODE=cloud

# Amorce Trust Directory URL (required for cloud mode)
# TRUST_DIRECTORY_URL=https://amorce-trust-api-425870997313.us-central1.run.app

# API Key for orchestrator authentication (optional)
# AGENT_API_KEY=sk-atp-amorce-2025-your-key-here

# Google Cloud Project ID (for Firestore and Secret Manager)
# GCP_PROJECT_ID=amorce-prod-rgosselin

# Private key storage in Google Secret Manager
# SECRET_NAME=atp-agent-private-key

# Redis Configuration (for rate limiting)
# REDIS_HOST=localhost
# REDIS_PORT=6379

# Agent ID (for smart_agent.py cloud mode)
# AGENT_ID=your-agent-uuid-here

# Gemini API Key (for AI features)
# GOOGLE_API_KEY=your-gemini-api-key

# -----------------------------------------------------------------------------
# OPTIONAL SETTINGS
# -----------------------------------------------------------------------------

# Server port (default: 8080)
# PORT=8080

# Log level (default: INFO)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO
